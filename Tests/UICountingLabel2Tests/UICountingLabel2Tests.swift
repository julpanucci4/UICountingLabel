import XCTest
@testable import UICountingLabel2

final class UICountingLabel2Tests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(UICountingLabel2().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
